angular.module("components.flights-table").controller("FlightsTableController", function () {
    var ctrl = this;

    ctrl.company = function (airCraft) {
        return ctrl.companies.find(function (c) {

        });
    }
});